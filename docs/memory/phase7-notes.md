# Phase 7 — Archived Memory Parity Notes

This file is intentionally no longer the memory-system reference.

Read [`README.md`](./README.md) instead. It is the SSOT for the current
global-only memory system:

- `~/.hlvm/HLVM.md` for user memory
- `~/.hlvm/memory/MEMORY.md` and `~/.hlvm/memory/*.md` for auto-memory
- no `./HLVM.md`
- no `~/.hlvm/projects/<key>/memory/`

Older versions of this note compared a per-project prototype against Claude
Code. That prototype was deleted during the global-only cleanup, so those paths
and fixture instructions are obsolete.
