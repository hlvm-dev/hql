// TypeScript module for import testing

export function tsMultiply(x: number): number {
  return x * 3;
}

export function tsAdd(a: number, b: number): number {
  return a + b;
}

export const TS_CONSTANT: string = "TypeScript works!";
