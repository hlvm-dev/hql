/**
 * Shared AI-facing user messages (SSOT).
 */

/** Fallback when a model produces no usable output. */
export const AI_NO_OUTPUT_FALLBACK_TEXT =
  "I couldn't generate a response. Please try again.";
