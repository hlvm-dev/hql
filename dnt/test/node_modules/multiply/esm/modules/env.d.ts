import { HQLValue } from "./type.js";
export declare class Env {
    bindings: Record<string, HQLValue>;
    outer: Env | null;
    exports?: Record<string, HQLValue>;
    constructor(bindings?: Record<string, HQLValue>, outer?: Env | null);
    get(key: string): HQLValue;
    set(key: string, value: HQLValue): void;
}
export declare const baseEnv: Env;
