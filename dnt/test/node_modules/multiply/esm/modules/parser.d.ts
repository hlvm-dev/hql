import { HQLValue } from "./type.js";
/**
 * Parse the HQL source input into an array of HQLValue AST nodes.
 * Each node now includes .start and .end offsets.
 */
export declare function parse(input: string): HQLValue[];
